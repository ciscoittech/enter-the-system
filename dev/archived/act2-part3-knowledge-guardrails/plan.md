# Act 2 Part III: Knowledge & Guard Rails (Ch 6-7)

## Goal

Reader adds Skill and Hook components. After Part III, 4 components working together. The .claude/ directory structure is introduced — skills and hooks live in real folders.

## Chapters

- **Ch 6** (Skill, Content System): Voice skill, editorial standards. Introduces `.claude/skills/` directory + SKILL.md format. Deep build on Content. Extend to Study (learning method skill), Job Hunting (career profile skill), PM (methodology skill). Include skill versioning maintenance.
- **Ch 7** (Hook, Job Hunting): Automated checks on cover letters. Introduces `.claude/settings.json` hook configuration + shell scripts in `.claude/hooks/`. Deep build on Job Hunting. Extend to all 4 systems. Include hook tuning maintenance.

## Quality Gate

- Both chapters score >= 20/30
- Reader understands .claude/ directory (skills + hooks + settings.json)
- Eval notebooks 03 (skill loading) and 04 (hook detection) run
